name: bitcoin-price-prediction
conda_env: conda.yaml 

entry_points:
  main:
    parameters:
      data_file: {type: path}
      epochs: {type: int, default: 5}
    command: "python modelling.py --data_file {data_file} --epochs {epochs}"
