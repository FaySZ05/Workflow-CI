name: bitcoin-price-prediction

conda_env: conda.yaml 

entry_points:
  main:
    parameters:
      data_file: {type: path, default: "MLProject/preprocessed_bitcoin_data.csv"}
      epochs: {type: int, default: 5}
    command: "python MLProject/modelling.py --data_file {data_file} --epochs {epochs}"
