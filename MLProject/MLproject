name: bitcoin-price-prediction

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_file: {type: path, default: "preprocessed_bitcoin_data.csv"}
    command: "python modelling.py --data_file {data_file}"
